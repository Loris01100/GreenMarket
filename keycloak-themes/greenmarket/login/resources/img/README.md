# Logo du thème de login GreenMarket

Dépose ici ton logo nommé `logo.png` (idéalement ~360px de large, fond transparent),
puis décommente le bloc `#kc-header-wrapper` dans `../css/greenmarket.css`.

Sans logo, la page affiche le `displayName` du realm GreenMarket par défaut.
